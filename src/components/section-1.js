import React from "react";

const Component1 = (props) => {
    const { /* First component object */ } = props;

    // Return Name, Email, Phone #
    return (
        <div>
        </div>
    );
}

export default Component1;